A program that uses an access database to access, and display students marks. 
There is a teacher login interface as well as a student login interface.
Microsoft Access based SQL database is used in this project.
